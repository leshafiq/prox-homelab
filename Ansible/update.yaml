---
- name: Update and upgrade system packages
  hosts: all
  become: true  # This is equivalent to 'sudo'
  
  tasks:
    - name: Run apt update
      apt:
        update_cache: yes
        cache_valid_time: 3600

    - name: Upgrade all packages to the latest version
      apt:
        upgrade: dist