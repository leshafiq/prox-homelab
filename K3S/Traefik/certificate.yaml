apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
   name: traefik-dashboard-certificate
   namespace: traefik
spec:
   secretName: traefik-certificate-secret
   dnsNames:
     - traefik.kube.xxx.com
   issuerRef:
     name: cloudflare-clusterissuer
     kind: ClusterIssuer